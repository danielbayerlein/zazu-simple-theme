{
  "name": "zazu-crystal-theme",
  "version": "1.0.0",
  "description": "A crystal theme for Zazu",
  "main": "dist/main.css",
  "scripts": {
    "build": "postcss src/main.css --use postcss-cssnext --dir dist --map",
    "watch": "npm run build -- --watch",
    "lint": "stylelint src/main.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/danielbayerlein/zazu-crystal-theme.git"
  },
  "keywords": [
    "zazu",
    "theme"
  ],
  "author": "Daniel Bayerlein",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/danielbayerlein/zazu-crystal-theme/issues"
  },
  "homepage": "https://github.com/danielbayerlein/zazu-crystal-theme#readme",
  "devDependencies": {
    "postcss-cli": "^3.0.0-beta",
    "postcss-cssnext": "^2.9.0",
    "stylelint": "^7.9.0",
    "stylelint-config-standard": "^16.0.0"
  }
}
